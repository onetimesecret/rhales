<schema lang="js-zod">
const schema = z.object({
  override_test: z.string(),
  child_only: z.string()
});
</schema>

<template>
  <div class="scope-override">
    <p>Override test in child: {{override_test}}</p>
    <p>Child only value: {{child_only}}</p>
    <p>Parent message accessible: {{parent_message}}</p>
    <p>User from context: {{user.name}}</p>
  </div>
</template>
