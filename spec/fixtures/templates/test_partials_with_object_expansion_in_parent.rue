<schema lang="js-zod" window="mainData">
const schema = z.object({
  key1_from_hash_object: z.string(),
  key2_from_hash_object: z.string(),
  key3_from_hash_object: z.string()
});
</schema>

<template>
  <div class="main-with-windows">

    <!-- SHOULD have a value -->
    <h1>{{key1_from_hash_object}}</h1>

    <!-- SHOULD have a value -->
    <p>{{key2_from_hash_object}}</p>

    <!-- SHOULD NOT have a value (b/c "mainData" is only relevant in the DOM, window[mainData]) -->
    <p>{{mainData.key2_from_hash_object}}</p>


    {{> test_partial_window3}}
  </div>
</template>
