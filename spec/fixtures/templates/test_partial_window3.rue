<schema lang="js-zod" window="partial3Data">
const schema = z.object({
  partial3_message: z.string()
});
</schema>

<template>
  <div class="partial3">

    <!-- SHOULD have a value -->
    <p>Main message in partial 3: {{key1_from_hash_object}}</p>

    <!-- SHOULD have a value -->
    <p>Partial 3 message: {{key2_from_hash_object}}</p>

    <!-- SHOULD have a value -->
    <p>Partial 3 message: {{key3_from_hash_object}}</p>

  </div>
</template>
